name: lab-gpu
ttl: 30m

tools:
  exec:
    commands:
      - nvidia-smi
      - python /opt/jobs/*
      - echo '*'
  file:
    read:
      - /datasets/**
    write:
      roots:
        - /workspace/**

limits:
  ttl: 30m
